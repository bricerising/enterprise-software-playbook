# Shared Skill References

This directory contains reference files used by multiple skills. Unlike per-skill `references/` subdirectories (e.g., `skills/architecture/references/`), files here are cross-cutting and linked from several skills.
